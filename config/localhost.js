module.exports = {
  // Server configuration
  server: {
    // Public URL from which the server can be accessed (used for persona)
    publicUrl:                      'http://localhost:60004',

    // Port to listen for requests on
    port:                           60004,

    // Run in development mode (logging and error handling)
    development:                    true
  }
};